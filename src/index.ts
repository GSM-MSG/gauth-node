import {
  issueCode,
  IssueCodeRequest,
  IssueCodeResponse,
} from '@/services/issueCode'

const Gauth = {
  issueCode,
}

export { issueCode, IssueCodeResponse, IssueCodeRequest }

export default Gauth
